import { makeKitLine } from '@agent-stack-ecosystem-kits/kit-core/theme';

export * from '@agent-stack-ecosystem-kits/kit-core/theme';

/** Colorize one `[mastra-kit]` framework log line. */
export const kitLine = makeKitLine('mastra-kit');
