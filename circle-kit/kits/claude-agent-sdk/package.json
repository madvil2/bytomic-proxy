{
  "name": "@agent-stack-ecosystem-kits/kit-claude-agent-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Autonomous Payment Agent example using the Claude Agent SDK + Circle Agent Stack.",
  "type": "module",
  "engines": {
    "bun": ">=1.2"
  },
  "scripts": {
    "demo": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agent-stack-ecosystem-kits/circle-tools": "workspace:*",
    "@agent-stack-ecosystem-kits/kit-core": "workspace:*",
    "@anthropic-ai/claude-agent-sdk": "^0.3.144",
    "dotenv": "^17.4.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
